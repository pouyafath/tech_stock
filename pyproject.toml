[tool.ruff]
target-version = "py311"
line-length = 140
extend-exclude = [
  ".venv",
  "build",
  "dist",
  "reports",
  "data",
  "temporary_upload",
]

[tool.ruff.lint]
select = ["E9", "F63", "F7", "F82", "I", "UP", "W"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
